task:
converting list api to the implementation followed by selection API in scenario_planner

points to understand:

points ignored:

points pending from execution/implementation:





steps to follow
1  understand the flow in selection API
2  understand the flow in list API
3  convert list api in to selection api implementation

at which step am i in the above steps?
1

flow in selection API:
it is implementing get scenarios method in service.py method
steps followed in get scenarios method
	fetches create_from_date, create_to_date from request object, if not found derives them
	fetches start_date, scenario_name from request object, if not found assigns None




what might go wrong:

steps to do:

steps to try:
1: 
2: 